# Fox AI Desktop App

## نصب و راه‌اندازی

### پیش‌نیازها
- Node.js (نسخه 16 یا بالاتر)
- npm
- سرور وب Fox AI در حال اجرا

### نصب
```bash
cd electron
npm install
```

### اجرا
```bash
# اول سرور وب را راه‌اندازی کنید
cd /home/hamed/personal-ai
source venv/bin/activate
python start_web.py

# سپس در ترمینال جدید
cd /home/hamed/personal-ai/electron
./start.sh
```

یا به صورت دستی:
```bash
cd electron
npm start
```

### ساخت فایل نصب
```bash
cd electron
npm run build
```

## ویژگی‌های Desktop App

- 🖥️ **رابط کاربری بومی** با منوهای سیستم
- 🔔 **اعلان‌های دسکتاپ** برای پیام‌های جدید
- ⌨️ **میانبرهای کیبورد** برای دسترسی سریع
- 🎨 **تم بهینه‌شده** برای دسکتاپ
- 🔒 **امنیت بالا** با context isolation
- 📱 **پشتیبانی از چند پلتفرم** (Windows, macOS, Linux)

## میانبرهای کیبورد

- `Ctrl+R` / `Cmd+R`: بازخوانی
- `F12`: ابزار توسعه‌دهنده
- `F11`: تمام صفحه
- `Ctrl+Q` / `Cmd+Q`: خروج
- `Ctrl+M` / `Cmd+M`: کمینه کردن

## ساختار فایل‌ها

- `main.js`: فرآیند اصلی Electron
- `renderer.js`: فرآیند رندر و preload
- `package.json`: تنظیمات و dependencies
- `about.html`: صفحه درباره
- `start.sh`: اسکریپت راه‌اندازی

## Build برای توزیع

```bash
# Windows
npm run build -- --win

# macOS  
npm run build -- --mac

# Linux
npm run build -- --linux

# همه پلتفرم‌ها
npm run build
```

فایل‌های نصب در پوشه `dist/` ساخته می‌شوند.
